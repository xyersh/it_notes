
### ISO -вариант
```sql
with  time_series(date_time) as (
    select :date1
    from dual

    union all

    select date_time + interval '1' hour
    from time_series
    where date_time + interval '1' hour < :date2
)

select*
from time_series
```


### Oracle-вариант
