
**YAML** (YAML Ain't Markup Language) — это человекочитаемый формат сериализации данных, предназначенный для представления структурированной информации. 
Основные преимущества:
-  Минималистичный синтаксис без скобок и запятых
-  Интуитивно понятная структура через отступы (как в Python)
-  Поддержка сложных структур данных (вложенные списки, словари)
-  Широко используется в конфигурационных файлах (Docker Compose, Kubernetes, Ansible, CI/CD)

> 💡 **Важно:** Расширение файла — `.yaml` или `.yml` (оба варианта допустимы).



## Советы и лучшие практики

✅ **Используйте 2 пробела** для отступов (стандарт де-факто)  
✅ **Всегда ставьте пробел после `:`**  
✅ **Кавычки — когда обязательны:** строки с `:`, `[`, `{`, `#`, начальные/конечные пробелы  
✅ **Валидируйте сложные файлы** через онлайн-валидаторы (например, yamlchecker.com)  
✅ **Используйте якоря** для повторяющихся блоков конфигурации  
✅ **Комментируйте сложные секции** для других разработчиков
✅ **Явные теги** применяйте только когда необходимо переопределить автоматическое поведение.


## Распространённые ошибки и как их избежать

| Ошибка                         | Пример                    | Исправление                 |
| ------------------------------ | ------------------------- | --------------------------- |
| Табуляция вместо пробелов      | `→host: localhost`        | `␣␣host: localhost`         |
| Отсутствие пробела после `:`   | `host:localhost`          | `host: localhost`           |
| Несогласованные отступы        | Смешение 2 и 4 пробелов   | Использовать один стиль     |
| Незакрытые кавычки             | `text: "hello`            | `text: "hello"`             |
| Двоеточие в строке без кавычек | `url: http://example.com` | `url: "http://example.com"` |

## Базовые правила синтаксиса

#### Отступы — основа структуры
```yml
# ПРАВИЛЬНО: отступы через пробелы (2 или 4 пробела)
server:
  host: 192.168.1.1
  port: 8080

# НЕПРАВИЛЬНО: табуляция или смешанные отступы
server:
	host: 192.168.1.1  # ← ошибка! табуляция вместо пробелов
```


#### Комментарии
```yml
# Это однострочный комментарий
port: 8080  # Комментарий после значения
```

## Типы данных и структуры
#### Скаляры (простые значения)
```yml
# Строки (кавычки необязательны, но иногда нужны)
name: John Doe
status: active
special: "value with : colon"  # кавычки нужны при спецсимволах
special2: 'another value' # одинарные кавычки также применимы при определении строки

# Числа
age: 30
price: 19.99
hex: 0xFF

# Булевы значения
active: true
active: True
deleted: false
deleted: False

# Null
middle_name: null
nickname: ~  # альтернативная запись null

# Даты и временные метки
created: 2024-02-13
timestamp: 2024-02-13T14:30:00Z
datetime_alt: 2024-02-13 14:30:00 +03:00
```

#### Списки (массивы)
```yml
# Стиль блока (рекомендуется)
fruits:
  - apple
  - banana
  - orange

# Стиль потока (в строку)
colors: [red, green, blue]
```

#### Словари (ассоциативные массивы / объекты)
```yml
# Вложенные структуры
database:
  host: localhost
  port: 5432
  credentials:
    username: admin
    password: secret123
```


#### Комбинация списков и словарей
```yml
users:
  - id: 1
    name: Alice
    roles: [admin, editor]
  - id: 2
    name: Bob
    roles: [viewer]
```

#### Явное указание типов

**Сводная таблица тегов**

|Тег|Тип|Пример|
|---|---|---|
|`!!str`|Строка|`!!str 123` → `"123"`|
|`!!int`|Целое число|`!!int "42"` → `42`|
|`!!float`|Число с плавающей точкой|`!!float "3.14"` → `3.14`|
|`!!bool`|Булево|`!!bool "true"` → `true`|
|`!!null`|Null|`!!null ""` → `null`|
|`!!timestamp`|Дата/время|`!!timestamp "2024-02-13"`|
|`!!binary`|Base64|`!!binary "SGVsbG8="` → `b"Hello"`|
|`!!set`|Множество|`!!set {a, b, c}`|

```yml
# Автоматическое определение типов
age: 30        # число
price: "30"    # строка из-за кавычек

# Принудительно строка
port: !!str 8080        # "8080", а не число 8080
version: !!str 1.2.3    # "1.2.3", а не float

# Принудительно число
count: !!int "42"       # 42 (число), а не строка "42"

# Принудительно булево
flag: !!bool "true"     # true (булево), а не строка "true"

# Принудительно float
ratio: !!float "3.14"   # 3.14 (число с плавающей точкой)

# Null
empty: !!null ""        # null, даже если строка выглядит как пустая строка

# Двоичные данные (base64)
certificate: !!binary |
  SGVsbG8gV29ybGQh

# Временная метка
event_time: !!timestamp "2024-02-13T14:30:00Z"

# Множественное значение (не поддерживается во всех парсерах)
options: !!set
  ? ruby
  ? perl
  ? python
```


## Многострочные строки

#### Блок-стиль (`|` — сохраняет переносы)
```yml
description: |
  Это многострочный текст.
  Каждая строка сохраняется как есть.
  Последний перенос тоже сохраняется.
```
Результат: `"Это многострочный текст.\nКаждая строка сохраняется как есть.\nПоследний перенос тоже сохраняется.\n"`

#### Свёрнутый стиль (`>` — заменяет переносы на пробелы)
```yml
summary: >
  Несколько строк будут
  объединены в одну.
  Двойной перенос = новый абзац.
```
Результат: `"Несколько строк будут объединены в одну. Двойной перенос = новый абзац."`

#### Управление конечными переносами
```yml
defaults: &defaults
  timeout: 30
  retries: 3
  debug: false

services:
  api:
    <<: *defaults  # Внедряем содержимое якоря
    port: 8080
    debug: true  # Переопределяем значение

  cache:
    <<: *defaults
    port: 6379
```


## Якоря и ссылки (DRY — Don't Repeat Yourself)
```yml
defaults: &defaults
  timeout: 30
  retries: 3
  debug: false

services:
  api:
    <<: *defaults  # Внедряем содержимое якоря
    port: 8080
    debug: true  # Переопределяем значение

  cache:
    <<: *defaults
    port: 6379
```
Результат:
```yml
services:
  api:
    timeout: 30
    retries: 3
    debug: true
    port: 8080
  cache:
    timeout: 30
    retries: 3
    debug: false
    port: 6379
```